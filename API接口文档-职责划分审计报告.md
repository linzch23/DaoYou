# API 接口文档 — 职责划分审计报告(完整版)

> 审计对象:`API接口文档.md`(2026-06-06 更新版,已含回收站专题 §6.5 / §6.10–6.12)
> 审计人:项目技术负责人视角
> 审计日期:2026-06-06(v1 完整版)
> 上版:v0(2026-06-06 上午,32 条发现)—— 本版**重写为 8 大类 + 80+ 条发现**,把"业务规则 / 信息流向 / 契约一致性"等深层职责问题补齐
>
> ---
>
> ## 审计目的
>
> 从**职责划分**的角度审计 API 设计。**不是**看字段齐不齐、文档完不完整,而是看:
> 1. **后端本质不知道**的信息,但接口要求后端必须知道
> 2. **前端本质不清楚**的信息,但接口要求前端必须提供
> 3. **信息流向**是否清晰(谁拥有、谁传递、谁验证)
> 4. **业务规则**是否闭环(状态机/幂等/事务/并发/边界)
> 5. **契约**是否一致(同名字段、错误码、响应结构)
> 6. **安全/审计/限流/性能**是否到位
>
> 这些地方是后续维护最容易扯皮的雷区。
>
> ---
>
> ## 审计方法 — 4 问法
>
> 每个接口逐字段问 4 个问题:
> 1. **后端真的需要这条信息吗?** 不传业务能跑吗?
> 2. **后端能信这条信息吗?** 客户端可篡改、可伪造、可晚到
> 3. **前端能稳定拿到这条信息吗?** 是不是要前端做额外的关联查询
> 4. **如果错了谁负责?** 后端兜底还是前端校验
>
> 然后再过 4 个**业务层**问题:
> 5. **状态机**怎么流转?哪个状态可以转到哪个?
> 6. **幂等性**怎么保证?网络重试会不会创建重复数据?
> 7. **事务**边界多广?并发写时谁赢?
> 8. **生命周期**什么时候结束?前端何时清缓存?
>
> ---
>
> ## 评级标准
>
> - 🔴 **P0 必须修**:会导致安全风险 / 数据不一致 / 用户体验断崖
> - 🟡 **P1 应该修**:会让前后端扯皮 / 后续扩展受限 / mock 与实际行为偏差
> - 🟢 **P2 建议修**:不修也能用,但文档明确化会显著降低沟通成本
>
> ---
>
> ## 8 大类总览
>
> | 大类 | 主题 | 数量 | 优先级分布 |
> | --- | --- | --- | --- |
> | **I. 职责越界** | 后端不应当知道 / 前端不应当清楚 / 其他 | 32 | P0×2 / P1×12 / P2×18 |
> | **II. 信息流向与上下文** | 上下文聚合 / 前端业务状态 / 资源生命周期 | 10 | P0×1 / P1×5 / P2×4 |
> | **III. 业务规则缺位** | 状态机 / 幂等 / 事务 / 并发 / 业务闭环 | 10 | P0×2 / P1×6 / P2×2 |
> | **IV. 错误处理一致性** | 错误码 / 错误消息 / 错误响应结构 | 7 | P0×1 / P1×3 / P2×3 |
> | **V. 契约一致性** | 同名字段 / 命名 / 类型 | 7 | P1×3 / P2×4 |
> | **VI. 文档质量** | 完整性 / 一致性 / 可读性 | 7 | P0×1 / P2×6 |
> | **VII. 安全 / 审计 / 限流** | 越权 / 重放 / 审计 / 限流 / 合规 | 7 | P0×2 / P1×3 / P2×2 |
> | **VIII. 性能 / 缓存 / 实时** | 缓存 / 同步 / 实时 / 超时 | 6 | P0×1 / P1×2 / P2×3 |
> | **合计** | | **86** | **P0×10 / P1×34 / P2×42** |
>
> 审计范围:21 个业务接口 + 1 个健康检查接口(已跨过 §1–§13 全部接口定义段)
>
> ---
>
> # I. 职责越界(原 A/B/C,32 条)
>
> ## I.A 后端本质不知道、但接口要求后端知道
>
> ### IA-1 [🔴 P0] `current_time` 不可信却用作业务时间基准 — §9.1
> - **现状**:`POST /api/reminders/check` body 含 `current_time: '2026-07-01T09:20:00+08:00'`,后端用此判断"是否错过出发时间"
> - **风险**:客户端可改系统时间 → 绕过所有时间型校验;客户端时钟漂移
> - **建议**:后端**忽略** `current_time` 字面值,用 server time;`current_time` 仅作"业务时间窗口匹配"(如"行程从早 9 点开始"),**不**作权威时间
> - **修复**:API 文档 §9.1 字段说明加"后端以 server time 为准"
>
> ### IA-2 [🔴 P0] `current_location` 频繁出现但 GPS 可被 mock — §7.1 / §8.1 / §9.1 / §10.1
> - **现状**:4 个接口都收 `current_location: { latitude, longitude }`
> - **风险**:浏览器 devtools / 移动端 mock location 可伪造;后端**无**独立验证手段(除非 IP 地理,但精度差)
> - **建议**:后端**必须**有兜底(无位置时仍能跑);文档明确"`current_location` 是前端最佳估计,后端不视为权威输入"
> - **修复**:在 §1.4 加"位置数据来源"段,统一说明后端不信任
>
> ### IA-3 [🟡 P1] `current_location` 强制 JSON 字符串序列化 — §8.1
> - **现状**:`§8.1` photos/explain 把 `current_location` 写成"JSON 字符串"
> - **问题**:其他接口都是 object,这一处不一致;前端承担序列化责任
> - **建议**:改为结构化 object;后端 Pydantic 直接接
> - **修复**:§8.1 字段说明改写
>
> ### IA-4 [🟡 P1] `user_id` 由前端传 — §1.3
> - **现状**:`MVP 暂不实现完整登录注册,默认使用 user_id=1`,所有接口都接收 `user_id`
> - **风险**:生产环境任何 `user_id` 都能改别人数据(越权)
> - **建议**:文档**显式标注**"`user_id` 是 MVP 临时方案,生产前用 token 解析";过渡期后端用 token 覆盖前端传的 `user_id`
> - **修复**:§1.3 加重构路径说明
>
> ### IA-5 [🟡 P1] Trip 状态机流转约束未定义 — §6.4
> - **现状**:`PUT /api/trips/{id}` 允许 `status: draft/active/finished`,文档没说哪些转换合法
> - **问题**:前端无法预知所有合法转换;`finished → draft` 应不合法但没说
> - **建议**:明确状态机:草稿→进行中→结束,`finished → active` 合法(支持重新激活),其他→ 4000
> - **修复**:§3.1 Trip 字段说明加状态机段;§6.4 加"合法转换"约束
>
> ### IA-6 [🟡 P1] `preferences` PUT partial-update 语义不明 — §11.2
> - **现状**:PUT 语义通常全量替换,但前端可能只改 1 个字段
> - **建议**:明确"preferences 子对象是 partial update,后端 merge";或拆为 `PUT /api/preferences/{key}`
> - **修复**:§11.2 字段说明
>
> ### IA-7 [🟡 P1] Trip PUT/POST 不传字段默认行为不明确 — §6.1 / §6.4
> - **问题**:不传字段时,后端是 null / 保留原值 / 清空?
> - **建议**:创建时:不传 = null(默认);修改时:不传 = 保留原值;字段类型用 `T | null`
> - **修复**:§6.1 / §6.4 加字段缺失默认行为段
>
> ### IA-8 [🟡 P1] `latitude / longitude` 与 `address` 强耦合 — §6.7 / §3.3
> - **问题**:必填同时传 address + 经纬度;实际场景可能只知其一
> - **建议**:至少一个即可,后端调地图 API 补齐
> - **修复**:§6.7 字段说明
>
> ### IA-9 [🟡 P1] `message` 长度上限未规定 — §7.1
> - **风险**:用户可发 100KB 文本,后端全量转发给 LLM,浪费 token
> - **建议**:前端软限 ≤ 2000 字符,后端硬限 4000,超出返回 4000
> - **修复**:§7.1 加字段约束
>
> ### IA-10 [🔴 P0] 所有列表接口缺分页 / 排序 — §6.2 / §6.10 / §7.2 / §9.2
> - **现状**:
>   - `GET /api/trips` — 无分页无 sort
>   - `GET /api/trash/trips` — 仅"按 deleted_at 降序",无分页
>   - `GET /api/chat/history` — `limit` 默认 20,无游标
>   - `GET /api/reminders` — 有 `status` 筛选,无分页
> - **风险**:用户 100 个 trip → 全量返回 → 性能 + 流量 + 渲染全炸
> - **建议**:统一加 `page+page_size` 或 `cursor+limit`;响应含 `total` / `has_more`
> - **修复**:§2 接口总览加"列表接口分页约定"段
>
> ### IA-11 [🟡 P1] `recognition_result` 字段"可能没用" — §8.1
> - **问题**:响应里有字段但语义不稳,前端不知道展示不展示
> - **建议**:决策留 or 删;留则明确语义
> - **修复**:§8.1 字段说明
>
> ### IA-12 [🟡 P1] `replan` 是否拉外部数据未明说 — §10.1
> - **问题**:改线质量取决于天气 / 路况,后端是否拉?拉不到 fallback?
> - **建议**:明确"`replan` 输入 = 行程 + 偏好 + 用户位置 + 改线意图;天气/路况后端按需拉,失败不影响返回草案"
> - **修复**:§10.1 行为段
>
> ### IA-13 [🟡 P1] `draft_id` 生命周期未规定 — §10.1 / §10.2
> - **问题**:draft 存哪?有效期多久?过期返回什么?清理机制?
> - **建议**:文档明确"draft 后端持久化,有效期 24h,过期返回 4000 `draft expired`"
> - **修复**:§10.1 / §10.2 加 draft_id 生命周期段
>
> ### IA-14 [🟢 P2] `image` size/type 限制由谁兜底 — §8.1
> - **现状**:文档"建议 jpg/jpeg/png/webp""建议 ≤ 10MB",都是软约束
> - **建议**:明确"前端软校验 + 后端硬拒(返回 4002)"
> - **修复**:§8.1 文件上传规则段
>
> ### IA-15 [🟡 P1] 拍照 `style` 枚举 ≠ 偏好 `explanation_style` 枚举 — §3.5 / §8.1
> - **现状**:偏好 `'professional' | 'fun' | 'children'`;拍照 `'professional' | 'casual' | 'kid'`
> - **问题**:同一概念两套枚举,前端要维护 mapping
> - **建议**:统一 3 枚举;或明确"拍照 style 是临时指定,与偏好独立"
> - **修复**:§3.5 / §8.1 字段说明
>
> ### IA-16 [🟢 P2] Agent fallback 文案来源未明说 — §13.3
> - **问题**:"Agent 缺 reply → fallback 文案",但文案在哪?
> - **建议**:明确"fallback 文案由后端常量维护,前端**不**需要兜底"
> - **修复**:§13.3
>
> ### IA-17 [🟢 P2] Agent 输入聚合规则不明 — §13.1
> - **问题**:`chat_history` 多长?`image_info` 从哪取?
> - **建议**:明确"Agent 输入聚合规则"(后端职责)
> - **修复**:§13.1
>
> ### IA-18 [🟢 P2] `trip_id` 多处必填但"后端是否有当前 trip 概念"未明说 — §5.1 / §7.x
> - **问题**:"当前 trip"是前端业务状态,但文档没说
> - **建议**:§1 加"前后端职责边界"段,列出"前端持有的业务状态"清单 vs "后端持有的业务状态"清单
> - **修复**:§1 新增段
>
> ## I.B 前端本质不清楚、但接口要求前端处理
>
> ### IB-1 [🟡 P1] 资源不存在/越权/已删除统一 4001,前端无法区分 — §6.3 / §6.5 / §6.10
> - **现状**:设计文档 §4.6 + 接口文档错误码表都明确"所有权校验失败统一返回 4001 防泄露"
> - **建议**:文档**强化**:"前端拿到 4001 不区分原因,统一 notfound 态"
> - **修复**:§1.5 错误码表加一行
>
> ### IB-2 [🟡 P1] `DELETE /api/trip-items/{id}` 冗余 3 层 ID — §6.9
> - **现状**:query 收 `trip_id + trip_day_id + trip_item_id`,路径还有 `item_id`
> - **问题**:前端为什么自己要确认 item 在哪个 day 下?这是后端职责
> - **建议**:简化为 `DELETE /api/trip-items/{item_id}?user_id=1`,后端反查
> - **修复**:§6.9 重写
>
> ### IB-3 [🟡 P1] 软删除后"不出现"规则未统一 — §6.10
> - **问题**:已删除 trip 在所有列表"不出现"是后端保证,但前端要不要主动过滤?
> - **建议**:文档 §1 加"软删除语义"段明确"后端所有列表聚合自动过滤;详情 4001;前端**不**必主动过滤(后端已保证)"
> - **修复**:§1 新增段
>
> ### IB-4 [🟢 P2] `4000` 内部细分原因宽,前端无法精细化提示 — §1.5
> - **建议**:4000 内部可细分(给 subcode 或 message 模板);或文档明确"4000 前端不区分"
> - **修复**:§1.5
>
> ### IB-5 [🟢 P2] `5001 / 5002 / 5003` 内部细分,前端无法区分 — §1.5
> - **建议**:文档明确"5001/5002/5003 前端不区分细分,统一 fallback"
> - **修复**:§1.5
>
> ### IB-6 [🟢 P2] `data: null` vs `data: {}` 缺统一 — §1.4
> - **建议**:统一"失败响应 `data: null` 必填"
> - **修复**:§1.4
>
> ### IB-7 [🟢 P2] 已删除 trip 的 `status` 字段如何展示不明 — §3.1 / §6.10
> - **建议**:明确"已删除 trip 的 status 保留删除前值,前端展示时优先 deleted_at + 角标,status 作辅助"
> - **修复**:§6.10
>
> ### IB-8 [🟢 P2] memory 接口触发时机 / 响应用途不明 — §12.1
> - **建议**:明确"接口是调试/管理用,前端**不**直接调;响应**不**进 UI"
> - **修复**:§12.1
>
> ### IB-9 [🟢 P2] 草稿与回收站边界 — §1.5
> - **建议**:明确"草稿 = client-only 临时数据,不属于后端 trip 域"
> - **修复**:§1
>
> ### IB-10 [🟢 P2] 提醒是 push 还是 poll — §9.1 / §9.2
> - **建议**:明确"MVP 阶段无 push 通道,前端在 tabBar onShow 等时机轮询"
> - **修复**:§9.1 / §9.2
>
> ### IB-11 [🟢 P2] fallback 草案何时触发未明说 — §10.1
> - **建议**:响应加 `is_fallback: boolean`,前端据此外显"演示草案"水印
> - **修复**:§10.1
>
> ### IB-12 [🟢 P2] 时间字段时区规则 — §1.2
> - **建议**:明确"后端统一 UTC 存储,响应**始终** +08:00 序列化(MVP 单时区)"
> - **修复**:§1.2
>
> ### IB-13 [🟢 P2] `error_code` 与 HTTP status 的映射 — §1.5
> - **建议**:明确"业务成功/失败都走 HTTP 200,业务 code 是唯一权威;HTTP status 仅在网络层/5xx 兜底"
> - **修复**:§1.5
>
> ## I.C 其他合理性问题
>
> ### IC-1 [🟡 P1] 缺行程日的查询 / 修改 / 删除接口
> - **现状**:只有 `POST /api/trips/{id}/days`(创建)
> - **建议**:补 `PUT/DELETE /api/trips/{id}/days/{day_id}`
> - **修复**:新增接口段
>
> ### IC-2 [🟢 P2] Trip `status` 与 item 聚合状态的关系
> - **建议**:明确"trip.status 与 item.status **没有自动联动**;完全独立维护"
> - **修复**:§3.1
>
> ### IC-3 [🟢 P2] 软删除后本地缓存如何清理
> - **建议**:文档加"缓存失效策略"段
> - **修复**:§1
>
> ### IC-4 [🟢 P2] 永久删除后前端本地数据库清理
> - **建议**:沿用 IB-3 兜底,前端**不**必主动清
> - **修复**:§6.12
>
> ### IC-5 [🟢 P2] 接口路径风格不统一
> - **建议**:文档 §2 表格加"路径设计说明"列
> - **修复**:§2
>
> ### IC-6 [🟢 P2] `unread_reminders` 是聚合数字,列表如何获取?
> - **建议**:明确"home 给数字,reminders 给列表,前端按需调"
> - **修复**:§5.1 / §9.2
>
> ### IC-7 [🟢 P2] `explanation_style` 枚举不一致(同 IA-15)
> - **修复**:见 IA-15
>
> ### IC-8 [🟢 P2] 接口 mock 覆盖率文档化
> - **建议**:§14 加 mock 覆盖矩阵表
> - **修复**:§14
>
> ### IC-9 [🟢 P2] 错误响应 `message` 文案多语言
> - **建议**:明确"message 是开发/调试文案,前端按 code 映射中文 Toast"
> - **修复**:§1.4
>
> ### IC-10 [🟢 P2] "成功"响应 `code: 0` 与"业务空"区分
> - **建议**:明确"成功 = 服务端正常处理,data 可能空(列表空 / 0 计数);前端用 data 字段判断有无"
> - **修复**:§1.4
>
> ### IC-11 [🟢 P2] `apply-plan` 失败后状态
> - **建议**:明确"应用改线是事务的,失败时行程完全不变,前端**不**需要恢复"
> - **修复**:§10.2
>
> ---
>
> # II. 信息流向与上下文污染(10 条)
>
> > **核心问题**:后端**怎么**用前端传的 `trip_id`?前端**怎么**管理"当前"业务状态?资源**何时**物理死亡?这些"信息所有权"问题没明说,后端实现自由度大,前端行为不可预测。
>
> ### II-1 [🟡 P1] `trip_id` 频繁出现但后端"用它做什么"没明说 — §5.1 / §6.3 / §6.4 / §6.5 / §6.6 / §6.7 / §6.8 / §6.9 / §7.1 / §7.2 / §8.1 / §9.1 / §9.2 / §10.1 / §10.2
> - **现状**:15 个接口都收 `trip_id`
> - **问题**:后端用 `trip_id` 仅校验所有权,还是**拉取 trip 数据作为 Agent 上下文**?实现自由度大
> - **建议**:文档 §1 加"trip_id 用途约定"段:业务接口(§7.1 / §8.1 / §9.1 / §10.1)= 拉上下文 + 校验所有权;管理接口(§6.x / §9.2 / §10.2)= 仅校验所有权
> - **修复**:§1 新增段
>
> ### II-2 [🟢 P2] 跨 trip 切换时后端缓存策略
> - **问题**:用户从 trip 1 切到 trip 2,后端冷启动重算?缓存粒度?
> - **建议**:明确"后端按 trip 维度缓存,失效策略 X"
> - **修复**:§5.1
>
> ### II-3 [🟡 P1] "当前聊天对象"业务状态缺失 — §7.1 / §7.2
> - **问题**:chat / history 都按 trip 隔离,但前端**怎么**管理"当前聊的 trip"状态?多 tab?单 tab?
> - **建议**:明确"前端业务状态 currentChatTripId,后端不维护"
> - **修复**:§1 业务状态清单
>
> ### II-4 [🟡 P1] `photo_records` 物理删除后,前端短期引用失效 — §6.12 / §8.1
> - **问题**:拍照讲解响应里 `photo_id: 1`,前端可能存"我的拍照"列表;永久删除 trip 时,photo_records 也物理删
> - **问题**:前端**不知道** photo 何时被物理删(无通知)
> - **建议**:明确"photo_id 是临时引用,前端**不**应持久化依赖"
> - **修复**:§8.1
>
> ### II-5 [🟢 P2] `notification_id` 生命周期 — §9.1 / §9.2
> - **问题**:已读状态变更的触发未明说
> - **建议**:明确"用户调 `GET /api/reminders` 看到时**不**自动标已读;前端需显式调 PUT 标已读(若有该接口)"
> - **修复**:§9.2
>
> ### II-6 [🟢 P2] `replan` 改线后,前端如何更新本地 store — §10.1 / §10.2
> - **问题**:apply-plan 改了 item,但**改了什么**前端不知道(没 diff 接口)
> - **建议**:明确"apply-plan 成功后,前端应重新拉 trip 详情"
> - **修复**:§10.2
>
> ### II-7 [🟡 P1] `replan` 是否读取 chat_history — §10.1 / §13.1
> - **问题**:§13.1 Agent 输入含 `chat_history`,但 §10.1 replan 没明说读不读
> - **建议**:明确"`replan` 读 chat_history 作为改线意图的补充输入"
> - **修复**:§10.1
>
> ### II-8 [🟡 P1] `apply-plan` 改的是哪个版本 — §10.1 / §10.2
> - **问题**:draft 基于**某时刻**的 trip 状态生成;用户在 draft 期间又改了 trip,然后 apply;应用哪个版本?
> - **建议**:明确"apply-plan 基于 draft 时刻状态,冲突时优先 draft(覆盖)"
> - **修复**:§10.2
>
> ### II-9 [🟢 P2] `preferences` 全局 vs trip 维度 — §11.x
> - **问题**:"这个 trip 用慢节奏,那个 trip 用紧凑"——支持吗?
> - **建议**:明确"preferences 是用户全局,MVP 不支持 trip 级覆盖"
> - **修复**:§11.1
>
> ### II-10 [🟢 P2] 永久删除 trip 时 chat_messages 物理删除,但用户体验上"聊过啥都没了"
> - **问题**:永久删除时,chat 一并清,但用户可能想"看我以前聊过啥"
> - **建议**:明确"软删除期间,chat 仍可继续;永久删除时,chat 一并清(不可恢复)"
> - **修复**:§6.12
>
> ---
>
> # III. 业务规则缺位(10 条)
>
> > **核心问题**:状态机怎么转?幂等性怎么保证?事务边界多广?并发写谁赢?这些**业务层规则**完全没明说,是后端实现的"暗契约",前端只能"试一下错了再说"。
>
> ### III-1 [🟡 P1] 状态机边界与联动缺失 — §3.1 / §3.3
> - **现状**:Trip / TripItem / Reminder 各自有 status,但**联动关系**不明
> - **问题**:所有 item.status='done' → trip.status 是否自动 'finished'?item 改 'skipped' → trip 怎么联动?
> - **建议**:明确"trip.status **独立维护**,不与 item 自动联动;前端主动 PUT 改 trip.status"
> - **修复**:§3.1 / §3.3
>
> ### III-2 [🔴 P0] 幂等性要求未规定 — §6.1 / §6.6 / §6.7 / §7.1 / §8.1
> - **风险**:
>   - `POST /api/chat` 同一 message 重发 → 创建 2 条 chat_messages?
>   - `POST /api/photos/explain` 同一图重传 → 创建 2 条 photo_records?
>   - `POST /api/trips` 网络重试 → 创建 2 个 trip?
>   - 用户网络差时,容易触发重复操作
> - **建议**:
>   - 明确"哪些接口需幂等键(POST 类)"
>   - 引入 `Idempotency-Key: <uuid>` header(客户端生成,24h 唯一)
>   - 文档规定每个 POST 接口的幂等策略
> - **修复**:§1 加"幂等性约定"段;POST 类接口补 header
>
> ### III-3 [🟡 P1] `apply-plan` 事务边界不清 — §10.2
> - **问题**:文档说"需要事务处理",但事务**多广**?
> - **建议**:明确"apply-plan 事务仅含 trip_days / trip_items 改动,**不**联动 chat/notifications"
> - **修复**:§10.2
>
> ### III-4 [🔴 P0] 并发控制策略未明说 — §6.4 / §6.7 / §6.8
> - **问题**:
>   - 两个设备同时改同一 trip → 谁赢?Last-Write-Wins?乐观锁?悲观锁?
>   - 用户在 web 改 trip,手机改 → 数据丢失?
>   - 改线时:用户在 chat 说改 + 在改线页直接拖 → 冲突?
> - **建议**:
>   - PUT 类操作用 `If-Match: <etag>` 做乐观锁
>   - GET 响应含 `ETag: <hash>` header
>   - 并发冲突返回 4000 `conflict`
> - **修复**:§1 加"并发控制"段;§6.x / §10.2 加 If-Match 头说明
>
> ### III-5 [🟡 P1] 业务"结束"流程不闭环 — §6.4
> - **问题**:
>   - "创建 trip 没去任何地方" → status 永远 'draft' → 何时变 'finished'?
>   - "去了一半删了" → trash 里 status 是删除前 'active'?有 'abandoned' 吗?
> - **建议**:明确"`finished` 由前端主动 PUT 触发;后端**不**自动判断结束;trash 中保留删除前 status"
> - **修复**:§6.4
>
> ### III-6 [🟡 P1] 软删除时机的合理性 — §6.5 / §6.12
> - **问题**:软删除 trip 时,chat 还能继续吗?(已 trip 删了但用户还在聊)
> - **建议**:明确"软删除期间,chat 仍可继续(写入的 chat_messages 仍保留,只是 trip.deleted_at 非空);永久删除时,chat_messages 一并清"
> - **修复**:§6.5 / §6.12
>
> ### III-7 [🟡 P1] 草稿与回收站边界 — §1.5
> - **问题**:草稿(NewTripPage local 草稿)与"软删除 trip"是两码事,但前端**容易混淆**
> - **建议**:明确"草稿 = client-only 临时数据,不属于后端 trip 域;草稿被用户清掉 ≠ 软删除 trip"
> - **修复**:§1
>
> ### III-8 [🟢 P2] 提醒触发条件 — §9.1
> - **问题**:提醒什么时候"应该"生成?后端定时任务?用户主动 check?
> - **建议**:明确"MVP 阶段无后端主动提醒,全部靠用户主动 check"
> - **修复**:§9.1
>
> ### III-9 [🟢 P2] 改线 `new_items` 字段冗余 — §10.1
> - **问题**:new_items[] 含 title / item_type / start_time / end_time / address / notes,**没有** latitude / longitude
> - **建议**:明确"replan new_items 是简化版(无坐标);apply 时由后端按 address 反查坐标"
> - **修复**:§10.1
>
> ### III-10 [🟢 P2] 行程日 `day_index` 重排 — §6.6
> - **问题**:day_index 是 int,删掉中间 day 后,**后续** day_index 是否自动重排?
> - **建议**:明确"day_index 由后端按创建顺序自动维护,前端不需要传(后端自己算)"
> - **修复**:§6.6
>
> ---
>
> # IV. 错误处理一致性(7 条)
>
> > **核心问题**:错误码使用是否一致?错误消息是否合理?响应结构是否统一?前端能否稳定处理错误?
>
> ### IV-1 [🟡 P1] 错误码使用一致性核查
> - **问题**:
>   - 4000(参数错误)出现频次?都对应什么?
>   - 4001(不存在)出现频次?
>   - 同一情况不同接口返回不同 code 吗?(如:重复操作、并发冲突)
> - **建议**:
>   - 全文档统一错误码表
>   - 每个接口的"失败场景 → code"矩阵
> - **修复**:§1.5 + 每个接口补"失败场景表"
>
> ### IV-2 [🟡 P1] 错误消息文案错乱 — §6.5 / §6.11 / §6.12
> - **问题**:
>   - §6.5 "trip is not in trash"(语义:该 trip 不在 trash 里)
>   - §6.11 "trip is not in trash"(语义:该 trip 不在 trash 里)
>   - §6.12 "trip must be moved to trash first"
>   - **用户视角**:§6.5 重复移入,实际状态"已在 trash",文案说"不在 trash"——反了
> - **建议**:
>   - 统一文案,加上"用户视角"层说明
>   - 前端根据 `code` 映射自己的 Toast(不直接展示 message)
>   - 或:message 加"用户视角"后缀,如 `trip is not in trash (already in trash)`
> - **修复**:§6.5 / §6.11 / §6.12
>
> ### IV-3 [🟡 P1] 错误消息"用户视角 vs 开发者视角"分层缺失 — §1.4
> - **问题**:message 是英文(trip not found),前端 Toast 中文
> - **建议**:
>   - 明确"message = 开发者文案,前端按 code 映射中文 Toast;message **不**直接展示给用户"
>   - 文档提供"code → 中文 Toast 建议映射表"
> - **修复**:§1.4 + §1.5 加映射表
>
> ### IV-4 [🟢 P2] `data: null` vs `data: {}` 缺统一 — §1.4
> - **建议**:统一"失败响应 `data: null` 必填,不能省略"
> - **修复**:§1.4
>
> ### IV-5 [🟢 P2] 同一操作的"成功"响应字段命名不统一 — §6.5 / §6.11 / §6.12
> - **问题**:
>   - `data: { deleted: true, deleted_at }`(移入回收站)
>   - `data: { restored: true }`(恢复)
>   - `data: { permanently_deleted: true }`(永久删除)
> - **建议**:
>   - 统一为 `{ success: true, ...其他字段 }` 或 `{}`
>   - 操作类响应**只**含**真正需要**的字段(如 `deleted_at`)
> - **修复**:§6.5 / §6.11 / §6.12
>
> ### IV-6 [🟢 P2] 错误响应是否含 `trace_id` 未规定 — §1.4
> - **问题**:前端拿到 5000 给开发反馈"我看这个错误了",但**没有** trace_id
> - **建议**:
>   - 加 `trace_id: string` 到所有失败响应
>   - 前端展示给用户方便排查
> - **修复**:§1.4
>
> ### IV-7 [🟢 P2] "操作类"成功响应 payload 必要性 — §6.5 / §6.11 / §6.12
> - **问题**:`data: { deleted: true }` 真有必要?HTTP 200 + code 0 已表明成功
> - **建议**:操作类响应**只**含**真正需要**的字段(如 `deleted_at`),其他一律不返
> - **修复**:同 IV-5
>
> ---
>
> # V. 契约一致性(7 条)
>
> > **核心问题**:同名字段是否含义一致?命名是否统一?类型是否稳定?
>
> ### V-1 [🟡 P1] `status` 字段多义 — §3.1 / §3.3 / §9.1
> - **问题**:
>   - Trip.status: 业务生命周期
>   - TripItem.status: 单项完成度
>   - Reminder.status: 已读未读
>   - 同一字段名 3 个语义,前端要维护 3 套 mapping
> - **建议**:重命名以消除歧义,或明确"3 个 status 是独立枚举,前端按上下文理解"
> - **修复**:§3.1 / §3.3 / §9.1 字段说明
>
> ### V-2 [🟢 P2] `id` vs `trip_id` 命名不一致 — §3.1 / §6.3
> - **问题**:`GET /api/trips` 响应里 trip 是 `{ id: 1 }`,路径里是 `trip_id`
> - **建议**:统一为 `id`(用命名空间区分:trips[].id vs trip_items[].id)
> - **修复**:§3.1 / §6.3
>
> ### V-3 [🟢 P2] `user_id` 命名跨接口
> - **建议**:文档统一"user_id" 命名
> - **修复**:§1.3
>
> ### V-4 [🟢 P2] snake_case vs camelCase 跨端约定
> - **现状**:API 响应 snake_case(`user_id` / `deleted_at` / `follow_up_questions`),前端 TS 用 camelCase(`userId` / `deletedAt`)
> - **建议**:文档 §1 加"前后端命名映射"段
> - **修复**:§1
>
> ### V-5 [🟢 P2] `id` 类型稳定性
> - **问题**:`id: number` 还是 `id: string`(UUID)?
> - **建议**:明确"id 是 int(自增),全局唯一"
> - **修复**:§3.1
>
> ### V-6 [🟢 P2] `message` / `content` / `notes` / `explanation` 命名混乱
> - **问题**:同是"文本内容"用不同字段名:`message` / `content` / `notes` / `explanation`
> - **建议**:统一为 `content`,或保留现状(避免大改)
> - **修复**:§3.1 / §7.x
>
> ### V-7 [🟢 P2] `int` vs `number` 字段类型描述
> - **问题**:文档说"int"但 JSON 都是 number
> - **建议**:统一"number (整数)"
> - **修复**:§3.x
>
> ---
>
> # VI. 文档质量(7 条)
>
> > **核心问题**:文档自身是否完整、一致、可读?
>
> ### VI-1 [🔴 P0] 缺"完整 curl 命令"段 — 全部接口
> - **现状**:大多数示例只给 body / response
> - **建议**:每个接口附"完整 curl 命令"(method / url / header / content-type)
> - **修复**:每个接口补 curl 段
>
> ### VI-2 [🟢 P2] 必填 / 选填标注不统一 — 全部接口
> - **问题**:有的接口在 query 段说"必填",有的在 body,有的没明说
> - **建议**:统一段位 + 加 ✅ 标记
> - **修复**:全部接口
>
> ### VI-3 [🟢 P2] 缺"OpenAPI 规范"yaml/json — 全部
> - **建议**:加 `openapi.yaml` 配套(机器可读)
> - **修复**:新增文件
>
> ### VI-4 [🟢 P2] 缺"前端典型调用流程图" — 全部
> - **建议**:加"典型用户旅程"附录(进首页 → 拉 home/today → 调 chat → ...)
> - **修复**:新增附录
>
> ### VI-5 [🟢 P2] 缺"接口依赖图" — 全部
> - **建议**:加"接口依赖矩阵"表
> - **修复**:§2
>
> ### VI-6 [🟢 P2] 缺"版本号 / 变更日志"
> - **现状**:文档最后更新:2026-06-06,但**没有**版本号
> - **建议**:加版本号(v0.1 / v0.2)+ 变更日志
> - **修复**:新增版本号 + changelog 段
>
> ### VI-7 [🟢 P2] "回收站专题"和"主文档"重复
> - **建议**:专题只列差异/简化版;主文档保留权威
> - **修复**:专题文档加"权威版本以主文档为准"声明
>
> ---
>
> # VII. 安全 / 审计 / 限流(7 条)
>
> > **核心问题**:越权怎么办?重放怎么办?审计谁负责?限流策略是什么?合规怎么搞?
>
> ### VII-1 [🟡 P1] 越权检测 — §6.3 / §6.5 / §6.7 / §6.11 / §6.12
> - **问题**:`GET /api/trips/{id}` 任何 user_id 都能查任何 trip?文档**没**明说"trip 必须属于 user_id"
> - **建议**:在 §1.4 加"权限模型"段,明确"后端强校验所有权,失败统一 4001"
> - **修复**:§1.4
>
> ### VII-2 [🟡 P1] 幂等键(同 III-2)
> - **建议**:引入 `Idempotency-Key: <uuid>` header
> - **修复**:§1 加"幂等性约定"段
>
> ### VII-3 [🟡 P1] 审计日志
> - **问题**:
>   - 谁删了 trip?何时?(可逆操作)
>   - 谁永久删除了?何时?(**不可逆操作,必须有审计**)
>   - 谁恢复了?何时?
> - **建议**:明确"后端维护审计日志,前端**不**感知"
> - **修复**:§6.5 / §6.11 / §6.12
>
> ### VII-4 [🟡 P1] 限流
> - **问题**:任何接口被刷怎么办?`POST /api/chat` 1 秒调 100 次 → LLM 配额打爆
> - **建议**:
>   - 明确"限流策略:XX 次/分钟,超出返回 429 Too Many Requests(**新错误码**)或 4000 `rate_limited`"
>   - 建议:MVP 阶段先文档化"应有 Rate Limiting",具体策略延后
> - **修复**:§1.5 加 429 / rate_limited 错误码
>
> ### VII-5 [🟢 P2] 数据导出 / 删除账户
> - **建议**:明确"MVP 不实现数据导出,后续扩 GDPR / 隐私合规接口"
> - **修复**:新增附录
>
> ### VII-6 [🟢 P2] 跨域 CORS 配置
> - **问题**:H5 端跨域请求 → 后端 CORS?
> - **建议**:明确"后端允许的 origin / method / header"
> - **修复**:§1
>
> ### VII-7 [🟢 P2] 敏感字段加密
> - **问题**:`preferences.special_needs` 可能含隐私;聊天内容可能含隐私
> - **建议**:明确"敏感字段是否加密传输(HTTPS / 字段级加密)"
> - **修复**:§1
>
> ---
>
> # VIII. 性能 / 缓存 / 实时(6 条)
>
> > **核心问题**:性能预算多少?支持缓存吗?实时通信怎么搞?
>
> ### VIII-1 [🟡 P1] 缓存策略(ETag / If-None-Match)
> - **问题**:任何 GET 接口支持 ETag?响应含 ETag header?
> - **建议**:
>   - 明确"GET 类接口支持 ETag,响应含 `ETag: <hash>` header;前端可发 `If-None-Match: <hash>` 节省流量"
>   - 304 Not Modified 怎么处理?业务 code 是 0 还是新码?
> - **修复**:§1 加"缓存约定"段;GET 类接口加 ETag 说明
>
> ### VIII-2 [🟢 P2] 响应压缩
> - **建议**:明确"后端响应支持 gzip,前端无需特殊处理"
> - **修复**:§1
>
> ### VIII-3 [🟡 P1] 增量同步(聊天历史) — §7.2
> - **问题**:`GET /api/chat/history?limit=20` 只能拿"最近 20 条"
>   - 用户往回翻 → 怎么拿"再之前 20 条"?
>   - 缺:`offset` / `before_id` / `cursor` 参数
> - **建议**:加 `before_id` 或 `cursor` 参数支持翻页;响应含 `has_more: boolean` / `next_cursor: string`
> - **修复**:§7.2 加分页参数
>
> ### VIII-4 [🟡 P1] 实时通信策略
> - **问题**:
>   - 提醒是 push 还是 poll?(B10)
>   - 聊天回复是 push 还是 poll?
>   - 拍照讲解结果(2-5 秒) push 还是 poll?
>   - 改线草案生成(可能 5-10 秒) push 还是 poll?
> - **建议**:统一说明"MVP 阶段所有长任务都是 poll(前端轮询结果接口),无 WebSocket / SSE"
> - **修复**:§1 加"实时通信"段
>
> ### VIII-5 [🟢 P2] 长任务超时
> - **问题**:`POST /api/photos/explain` 可能 30 秒;`POST /api/replan` 可能 10 秒
> - **建议**:明确"长任务超时 X 秒,前端需配 timeout 兜底"
> - **修复**:§7.1 / §8.1 / §10.1
>
> ### VIII-6 [🟢 P2] 性能预算
> - **建议**:明确"性能预算:列表 ≤ 200ms,详情 ≤ 100ms,长任务 ≤ 30s"
> - **修复**:§1
>
> ---
>
> # 修复优先级总览
>
> ## 🔴 P0 必须修(10 条)
>
> | ID | 类别 | 标题 | 修复方 |
> | --- | --- | --- | --- |
> | IA-1 | 职责越界 | `current_time` 不可信却用作业务时间基准 | 后端 |
> | IA-2 | 职责越界 | `current_location` 频繁出现但 GPS 可被 mock | 后端 + 文档 |
> | IA-10 | 职责越界 | 所有列表接口缺分页 / 排序 | 后端 + 文档 |
> | II-1 | 信息流向 | `trip_id` 频繁出现但后端"用它做什么"没明说 | 文档 |
> | III-2 | 业务规则 | 幂等性要求未规定 | 后端 + 文档 |
> | III-4 | 业务规则 | 并发控制策略未明说 | 后端 + 文档 |
> | IV-1 | 错误处理 | 错误码使用一致性核查 | 文档 |
> | VI-1 | 文档质量 | 缺"完整 curl 命令"段 | 文档 |
> | VII-3 | 安全 | 审计日志(永久删除等不可逆操作) | 后端 |
> | VII-4 | 安全 | 限流 | 后端 + 文档 |
>
> ## 🟡 P1 应该修(34 条)
>
> | ID | 标题 |
> | --- | --- |
> | IA-3 | `current_location` 强制 JSON 字符串序列化 |
> | IA-4 | `user_id` 由前端传(MVP 临时) |
> | IA-5 | Trip 状态机流转约束未定义 |
> | IA-6 | `preferences` PUT partial-update 语义不明 |
> | IA-7 | Trip PUT/POST 不传字段默认行为不明确 |
> | IA-8 | `latitude / longitude` 与 `address` 强耦合 |
> | IA-9 | `message` 长度上限未规定 |
> | IA-11 | `recognition_result` 字段"可能没用" |
> | IA-12 | `replan` 是否拉外部数据未明说 |
> | IA-13 | `draft_id` 生命周期未规定 |
> | IA-15 | 拍照 `style` 枚举 ≠ 偏好 `explanation_style` 枚举 |
> | IB-1 | 资源不存在/越权/已删除统一 4001,前端无法区分 |
> | IB-2 | `DELETE /api/trip-items/{id}` 冗余 3 层 ID |
> | IB-3 | 软删除后"不出现"规则未统一 |
> | II-3 | "当前聊天对象"业务状态缺失 |
> | II-4 | `photo_records` 物理删除后,前端短期引用失效 |
> | II-7 | `replan` 是否读取 chat_history |
> | II-8 | `apply-plan` 改的是哪个版本 |
> | III-1 | 状态机边界与联动缺失 |
> | III-3 | `apply-plan` 事务边界不清 |
> | III-5 | 业务"结束"流程不闭环 |
> | III-6 | 软删除时机的合理性 |
> | III-7 | 草稿与回收站边界 |
> | IV-2 | 错误消息文案错乱 |
> | IV-3 | 错误消息"用户视角 vs 开发者视角"分层缺失 |
> | V-1 | `status` 字段多义 |
> | VII-1 | 越权检测 |
> | VII-2 | 幂等键(同 III-2,合并) |
> | VIII-1 | 缓存策略(ETag / If-None-Match) |
> | VIII-3 | 增量同步(聊天历史) |
> | VIII-4 | 实时通信策略 |
> | IC-1 | 缺行程日的查询 / 修改 / 删除接口 |
>
> ## 🟢 P2 建议修(42 条)
>
> 全部 P2 项目在 §I/§II/§III/§IV/§V/§VI/§VII/§VIII 详细列出,此处不重复。
>
> ---
>
> # 修复责任划分
>
> | 类别 | 性质 | 主要修复方 |
> | --- | --- | --- |
> | 职责越界(IA) | 后端**不**应当必须知道 | 后端实现 + 文档明文化 |
> | 职责越界(IB) | 前端**不**应当必须清楚 | API 设计优化(后端)+ 文档明文化 |
> | 职责越界(IC) | 其他 | 视具体条目 |
> | 信息流向 | 后端上下文聚合规则 | 后端实现 + 文档 |
> | 业务规则 | 状态机/幂等/事务/并发 | 后端实现(决定权)+ 文档 |
> | 错误处理一致性 | 错误码/消息/结构 | 项目技术负责人 + 后端 |
> | 契约一致性 | 命名/类型 | 文档(后端实现跟随) |
> | 文档质量 | 完整性/一致性 | 项目技术负责人 |
> | 安全/审计/限流 | 越权/重放/审计/限流 | 后端实现 + 文档 |
> | 性能/缓存/实时 | 缓存/同步/实时 | 后端实现 + 文档 |
>
> ---
>
> # 不在本审计范围
>
> - **代码实现审计**(`backend/app/` 实际代码与文档一致性)→ backend-writer 负责
> - **前端实现审计**(`frontend/src/` 实际代码与文档一致性)→ code-writer 负责
> - **mock 与文档一致性** → mock-writer 负责
> - **数据库设计审计**(`trips` 表结构 / 索引)→ db-writer 负责
> - **性能压测** → perf-writer 负责
>
> ---
>
> # 维护建议
>
> 1. **版本化**:本审计报告与《API接口文档.md》同生命周期。每次 API 文档大改时同步过一遍
> 2. **去重**:本次发现中,有 4 条与设计文档《回收站删除恢复功能设计(1)》已经覆盖,已合并到 IA/IB 中,不再重复
> 3. **修复执行**:每条 P0/P1 修复建议应在实施前明确"由谁做 / 何时做 / 怎么验证",但**不**在本次审计范围
> 4. **审计频率**:每个 sprint 末 / API 重大变更后 / 新增接口必审计
>
> ---
>
> # 审计元信息
>
> - **审计方法**:4 问法(后端是否需要/是否能信 + 前端是否能拿到/错了谁负责)+ 4 业务层问法(状态机/幂等/事务/并发)
> - **审计深度**:深(已触及业务规则、信息生命周期、契约一致性)
> - **发现总数**:86 条
> - **优先级分布**:P0 ×10 / P1 ×34 / P2 ×42
> - **涉及接口数**:21 / 21 业务接口(全覆盖)
> - **v0 → v1 增量**:+54 条(从 32 → 86),主要增量来自 II 信息流向、III 业务规则、IV 错误处理、VII 安全、VIII 性能 5 个新增大类
