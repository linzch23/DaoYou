version: 1
plan:
  name: 'disable-mock-default 跨页 fix(code-writer 改 services/config.js 1 行)'
  max_concurrency: 1
  max_consecutive_failures: 2
  max_cycles: 2
  auto_accept: true
  auto_reject_retries: 1
  verifier_config:
    default_verifiers:
      - verifier
    audit_sample_rate: 0
    strict_mode: false
tasks:
  - id: disable-mock-default-code
    title: 改 USE_MOCK_FALLBACK = false(1 行)+ 写 deliverable + 改 PageStatus
    prompt: |
      ## 任务

      修 Cross-Page Issue `disable-mock-default-2026-06-18.md`(Critical,FixQueue 队首)。

      ## 根因(1 行总结)

      `src/services/config.js:36` 当前 `export const USE_MOCK_FALLBACK = true`,dev 模式默认装 mock 拦截器 + HTTP 失败时静默降级 mock。改 false 后**两个挡板同时关掉**:
      1. `src/main.js:34` 第一个条件 `USE_MOCK_FALLBACK` 为 false → mock 拦截器永远不装
      2. 4 个 service 的 `isFallbackable` 第一行 `if (!USE_MOCK_FALLBACK) return false` → HTTP 失败永远不降级 mock

      ## 必读(3 段 5min 内读完)

      - `issues/Cross-Page/disable-mock-default-2026-06-18.md`(完整根因 + 验收 + fix-only 协议)
      - `outputs/connect-real-backend/notes.md` §5.2(orchestrator 写的工单草稿)
      - `src/services/config.js`(要改的文件,1 行)
      - `src/main.js:34-35`(拦截器安装条件,看不改)
      - `src/services/preferences.js:120-123`(`isFallbackable` 范式,其他 3 service 同理,看不改)

      ## 改动(1 行,严禁扩 scope)

      改 `src/services/config.js:36`:
      ```diff
      - export const USE_MOCK_FALLBACK = true
      + export const USE_MOCK_FALLBACK = false
      ```

      **严禁改其他文件**(per Issue §"不做的事" + AGENTS.md §6 硬规则):
      - ❌ `src/main.js`(拦截器安装条件保持)
      - ❌ `src/utils/mockInterceptor.js`(仍存在,只是永远不装)
      - ❌ `specs/*` 任何文件
      - ❌ `pages/*` / `components/*` / `stores/*` 任何文件
      - ❌ `api/mock/*` 任何文件
      - ❌ 不加新功能
      - ❌ 不动 Issue 文件(`issues/Cross-Page/disable-mock-default-2026-06-18.md` 保持原状,code-writer 只能改 `Status: Open → InProgress` 但本任务**不**写 Status 字段,因为是 orchestrator 立 Issue,不是 code-writer 立的)

      ## 必做 5 步(5min budget)

      1. **改 1 行**:`Edit src/services/config.js:36` `true → false`
      2. **写 deliverable.md**:`outputs/disable-mock-default/deliverable.md`(5 章节,1:1 对齐 Issue "deliverable.md 必含 5 章节"段)
         - §1 diff
         - §2 4 service 行为分析(`isFallbackable` 范式 × 4,改 false 后行为变化)
         - §3 mock 拦截器 0 装证据(`main.js:34` 第一个条件 false 推导)
         - §4 escape hatch 兼容(`window.__USE_REAL_API__ = true` 仍可用,向后兼容 dev 手动模式)
         - §5 fix-only 协议说明(per AGENTS.md §8.6 + §8.7 + memory §6a,**不**触发 ui-reviewer / spec-auditor / test-agent)
      3. **改 PageStatus.yaml** 追加 1 个块(per AGENTS.md §8.6 retro fix 协议):
         ```yaml
         # ============================================================
         # Cross-Page: disable-mock-default  ── 2026-06-18 fix
         # ============================================================
         - Cross-Page: disable-mock-default

           Spec:
             status: NotApplicable
             note: 'Cross-Page 基础设施级 config 变更,无 page 块对应;0 spec 字段变更'
           Architecture:
             status: NotApplicable
             note: '0 架构字段变更'
           Development:
             status: Completed
             note: '2026-06-18 改 src/services/config.js:36 true→false(1 行),跨页影响 4 service + 13 page 行为;0 spec 字段变 + 0 API 契约变 + 0 新功能'
           Review:
             ui: Pass
             note: 'fix-only 协议(per AGENTS.md §8.6 + §8.7 + memory §6a):0 spec 字段变更 + 0 API 契约变更 + 0 新功能 → 统一保持 Pass,不走完整 ui-review'
             spec: Pass
             note: 'fix-only 协议:同上,不走完整 spec-auditor'
             test: Pass
             note: 'fix-only 协议:同上,不走完整 test-agent'
           FinalStatus: Done
             note: '2026-06-18 orchestrator 聚合 Done,5/5 满足(Spec/Arch/Development/3 Review);fix-only 路径,0 reviewer 触发,3 Review 字段 orchestrator 显式标 Pass'
         ```
         注意:此块要 append 到 PageStatus.yaml 末尾,**不**触动既有 13 page 块(per AGENTS.md §8.6 "0 触动既有 page 块" + memory §6a "plan 内 PageStatus 必须一致")
      4. **跑端到端验证**:`bash outputs/connect-real-backend/verify.sh` 应 8/8 PASS(后端 + DB + API 全通)
      5. **写完后立即退出**(per AGENTS.md §2.4 code-writer 硬规则"一次只处理一个页面;处理完立即退出")

      ## 5min budget 硬约束

      这是 1 行改动,5min 内必须完成。如果 5min 内没完,回看 Issue + notes.md 找根因,**不要瞎改其他文件**(per Issue "不做的事"段)。

      ## 失败时回退

      如果 deliverable.md / PageStatus.yaml / verify.sh 任何一步失败:
      1. 立刻停手,不再写其他文件
      2. 在 deliverable.md 顶部加 1 行 `STATUS: BLOCKED` + 简述卡哪
      3. 退出,等 orchestrator 介入

      ## 不要做的事(再次强调)

      - ❌ 不要 re-read 全 spec 全文
      - ❌ 不要重写 deliverable.md 的 5 章节
      - ❌ 不要动既有 13 page PageStatus 块
      - ❌ 不要触发 ui-reviewer / spec-auditor / test-agent
      - ❌ 不要改 `main.js` / `mockInterceptor.js` / `specs/*` / `pages/*` / `components/*` / `stores/*` / `api/mock/*`
    verify_prompt: |
      独立验证 1 行 fix 是否到位(per Issue "验收脚本"段)。

      必查项(7 项,1:1 对齐 Issue "验收(必做 5 项)"段 + 2 项额外):

      1. `grep -n "USE_MOCK_FALLBACK" src/services/config.js` → 应输出 `36:export const USE_MOCK_FALLBACK = false`(false 不是 true)
      2. `grep -rln "USE_MOCK_FALLBACK" src/` → 应只输出 `config.js` / `main.js` / 4 个 service 文件,**不**出现新文件
      3. `ls outputs/disable-mock-default/deliverable.md` → 文件存在
      4. `outputs/disable-mock-default/deliverable.md` 含 5 章节(grep `^##` 应至少 5 行)
      5. `bash outputs/connect-real-backend/verify.sh` → 8/8 PASS(实测)
      6. `grep -A 1 "Cross-Page: disable-mock-default" workflow/PageStatus.yaml` → 块存在
      7. **不**改其他文件(对比 `git status` 或 `find src -newer <deliverable>`,确认只有 `config.js` 1 个改动)

      **不通过**: 列具体哪项 fail + grep/wc -l 实际输出
    assigned_to: users-andrew-desktop-vivo-daoyou-frontend--code-writer
    role: produce
    verified_by: verifier
    depends_on: []
    gates: []
    max_retries: 2
    timeout_ms: 600000
    hang_alert_after_ms: 300000
