# 《导友——你的个人旅游搭子》MVP 规格说明

## 1. 项目定位

《导友》是一款基于大模型与 LangGraph 的主动式旅游陪伴 Agent。它不是普通攻略工具，也不是单次问答机器人，而是围绕用户旅行全过程提供行前计划、行中陪伴、拍照讲解、智能提醒、动态改线和个性化记忆的应用。

MVP 的目标是在 5 周内完成一个可运行、可联调、可录制复赛视频的演示系统，证明“主动式旅游 Agent”在真实旅行场景中的价值。

## 2. MVP 成功标准

| 维度 | 标准 |
| --- | --- |
| 功能闭环 | 创建旅行、今日行程、AI 对话、图片讲解、提醒、改线均可演示 |
| 稳定性 | 核心演示路径连续运行 5 分钟不崩溃 |
| 工程性 | 前后端分离，API 格式统一，数据库结构清晰 |
| Agent 能力 | 能读取行程和偏好，输出有上下文的建议 |
| 演示效果 | 评委能看出它区别于静态攻略和 OTA AI 问答 |

## 3. 核心用户

| 用户类型 | 特征 | 需求 |
| --- | --- | --- |
| 自由行青年 | 大学生、年轻白领、周末游用户 | 快速制定行程、灵活改线、拍照讲解 |
| 亲子游家庭 | 家长带儿童出游 | 通俗讲解、节奏舒适、安全提醒 |
| 银发文化游用户 | 中老年文化旅行者 | 操作简单、少步行、讲解清晰 |
| 深度旅行用户 | 喜欢小众路线和文化体验 | 个性化讲解、持续记忆和推荐 |

## 4. 核心演示路径

```text
创建“大连三日游”
→ 首页展示今日行程
→ 上传景点图片
→ AI 生成景点讲解
→ 用户追问拍照建议
→ 检查行程风险并生成出发提醒
→ 用户提出“不想去下一个景点”
→ Agent 生成轻松版路线
→ 用户确认后更新行程
```

## 5. 功能规格

### 5.1 行程管理

用户可以创建旅行计划、创建每日行程、添加景点节点、修改节点、删除节点，并在首页查看当日时间轴。

验收标准：

- 可创建“大连三日游”。
- 可添加至少 3 个行程节点。
- 首页能按时间展示今日行程。
- 修改和删除节点后，详情页和首页数据同步。

### 5.2 AI 对话

用户可以向导友提问，后端读取当前行程、用户偏好和历史消息，调用 LangGraph Agent 生成回复。

验收标准：

- `/api/chat` 能返回带上下文的回复。
- 聊天记录能保存和查询。
- 回复能体现当前行程，例如时间是否紧张、下一站是否太远。
- 大模型失败时有 fallback 文案，不影响演示流程。

### 5.3 拍照讲解

用户上传图片后，系统保存图片，调用图片理解或 OCR 工具，结合偏好生成景点、建筑、展品或文字讲解。

验收标准：

- `/api/photos/explain` 支持 `multipart/form-data`。
- 图片保存到 `backend/uploads/images/`。
- 返回识别结果、讲解内容和追问建议。
- 文件类型、大小和保存路径有基本校验。

### 5.4 智能提醒

MVP 使用“半主动提醒”：用户点击“检查行程风险”，系统读取今日行程、当前位置和当前时间，判断是否存在出发或时间冲突风险。

验收标准：

- `/api/reminders/check` 能生成提醒或返回暂无风险。
- 提醒记录写入 `notifications`。
- `/api/reminders` 能查询提醒列表。
- 优先支持出发提醒和时间冲突提醒。

### 5.5 动态改线

用户通过自然语言提出临时变化，Agent 读取当前行程和偏好，调用地图工具或模拟数据生成替代方案。方案生成阶段不直接覆盖原行程，必须等待用户确认。

验收标准：

- `/api/trips/{trip_id}/replan` 返回改线草案。
- 草案包含推荐理由和新行程节点。
- `/api/trips/{trip_id}/apply-plan` 在用户确认后更新 `trip_items`。
- 取消草案不会修改原行程。

### 5.6 用户偏好与记忆

MVP 记录讲解风格、旅行节奏、兴趣偏好和特殊需求，用于影响讲解、对话、提醒和改线。

验收标准：

- 支持读取和更新偏好。
- 至少支持 `explanation_style`、`travel_pace`、`interests`、`special_needs`。
- AI 回复能体现偏好，例如趣味讲解、慢节奏、少步行。

## 6. API 规格

所有接口返回统一格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

错误码：

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 4000 | 请求参数错误 |
| 4001 | 资源不存在 |
| 4002 | 文件上传失败 |
| 5000 | 后端服务错误 |
| 5001 | 大模型调用失败 |
| 5002 | 地图 API 调用失败 |
| 5003 | Agent 输出解析失败 |

核心接口：

| 模块 | 方法与路径 | 说明 |
| --- | --- | --- |
| 健康检查 | `GET /health` | 检查后端服务是否启动 |
| 首页 | `GET /api/home/today` | 获取今日行程和未读提醒 |
| 行程 | `POST /api/trips` | 创建旅行 |
| 行程 | `GET /api/trips` | 获取旅行列表 |
| 行程 | `GET /api/trips/{trip_id}` | 获取旅行详情 |
| 行程 | `PUT /api/trips/{trip_id}` | 更新旅行 |
| 行程 | `DELETE /api/trips/{trip_id}` | 删除旅行 |
| 行程 | `POST /api/trips/{trip_id}/days` | 创建行程日 |
| 行程 | `POST /api/trip-items` | 创建行程节点 |
| 行程 | `PUT /api/trip-items/{item_id}` | 更新行程节点 |
| 行程 | `DELETE /api/trip-items/{item_id}` | 删除行程节点 |
| 对话 | `POST /api/chat` | 发送消息并获取 Agent 回复 |
| 对话 | `GET /api/chat/history` | 查询聊天历史 |
| 拍照 | `POST /api/photos/explain` | 上传图片并生成讲解 |
| 提醒 | `POST /api/reminders/check` | 检查行程风险 |
| 提醒 | `GET /api/reminders` | 查询提醒列表 |
| 改线 | `POST /api/trips/{trip_id}/replan` | 生成改线草案 |
| 改线 | `POST /api/trips/{trip_id}/apply-plan` | 应用改线方案 |
| 偏好 | `GET /api/preferences` | 查询偏好 |
| 偏好 | `PUT /api/preferences` | 更新偏好 |
| 记忆 | `POST /api/memory/summary` | 总结用户记忆 |

## 7. 数据规格

MVP 数据表：

- `users`：用户，MVP 可默认 `user_id=1`。
- `trips`：旅行计划。
- `trip_days`：旅行中的某一天。
- `trip_items`：具体景点、餐饮、休息或交通节点。
- `chat_messages`：用户与 AI 的聊天记录。
- `user_preferences`：显式偏好。
- `user_memory`：从对话中总结的长期记忆。
- `photo_records`：图片上传、识别结果和讲解记录。
- `notifications`：提醒记录。

## 8. 非功能规格

- 演示优先：复杂商业化能力让位于稳定演示闭环。
- 可追溯：核心业务数据写入 PostgreSQL，便于演示回放和排查。
- 可降级：外部大模型、地图、图片理解失败时返回 fallback。
- 可联调：Swagger 可查看接口，字段命名稳定。
- 可维护：路由、schema、service、model、agent 分层清晰。

## 9. MVP 不做事项

- 不做完整登录注册和支付。
- 不接入真实 OTA 下单。
- 不强依赖 Redis、MinIO 和消息队列。
- 不做复杂后台推送，提醒采用用户主动检查。
- 不训练自有模型，使用外部大模型和多模态 API。

